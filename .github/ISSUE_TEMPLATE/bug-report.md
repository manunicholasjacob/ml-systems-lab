---
name: Bug report
about: Something failed, hung, or produced a number you can show is wrong
title: ""
labels: bug
---

**What happened:**

**Command and config** (the `matrix` block is usually enough):

```
(paste here)
```

**The record it produced**, if any. Failed runs are written to disk too, with `status`,
`error`, and the raw output; that file is usually the whole diagnosis:

```
(paste here)
```

**Platform:** OS, Python version, and for device bugs the output of `mlsys probe`.
